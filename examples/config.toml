# Add this table to ~/.codex/config.toml after running install.ps1.
# Restart Codex after saving it.

[mcp_servers.codex-kb]
command = "codex-kb"
args = ["mcp"]
startup_timeout_sec = 10
tool_timeout_sec = 20

